<style>
  .ag { display:flex; flex-direction:column; min-height:0; font-size:12px; }
  .ag-bar { display:flex; align-items:center; gap:6px; padding:5px 8px; border-bottom:1px solid var(--line-1); flex-shrink:0; }
  .ag-row-grid { display:grid; grid-template-columns: var(--ag-cols); align-items:center; }
  .ag-head { position:sticky; top:0; z-index:1; background:var(--bg-2); border-bottom:1px solid var(--line-1); }
  .ag-th { padding:4px 8px; font-size:10px; font-weight:600; text-transform:uppercase; letter-spacing:0.05em; color:var(--fg-3); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; user-select:none; }
  .ag-row { height:32px; border-bottom:1px solid var(--line-0); cursor:pointer; }
  .ag-td { padding:0 8px; font-size:12px; color:var(--fg-1); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
</style>