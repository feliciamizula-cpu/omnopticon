name: CD - Deploy Web Only

on:
  push:
    branches: [main]
    paths:
      - "src/Argus.Web/**"

permissions:
  id-token: write
  contents: read

env:
  DEPLOY_IMAGE_TAG: staging

concurrency:
  group: deploy-gke-web
  cancel-in-progress: false

jobs:
  build-and-deploy-web:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: project-30b3b95e-ed2b-4573-98a

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker build layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-web-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-web-
            ${{ runner.os }}-buildx-

      - name: Build and push Argus.Web image
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --file deploy/Dockerfile.service \
            --build-arg PROJECT_PATH=src/Argus.Web/Argus.Web.csproj \
            --build-arg ASSEMBLY_NAME=Argus.Web \
            --build-arg GIT_SHA=${{ github.sha }} \
            --build-arg BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ") \
            --cache-from type=local,src=/tmp/.buildx-cache \
            --cache-to type=local,dest=/tmp/.buildx-cache-new,mode=max \
            --cache-from type=registry,ref=us-central1-docker.pkg.dev/project-30b3b95e-ed2b-4573-98a/argus/argus-web:buildcache \
            --cache-to type=registry,ref=us-central1-docker.pkg.dev/project-30b3b95e-ed2b-4573-98a/argus/argus-web:buildcache,mode=max \
            --tag us-central1-docker.pkg.dev/project-30b3b95e-ed2b-4573-98a/argus/argus-web:${{ env.DEPLOY_IMAGE_TAG }} \
            --tag us-central1-docker.pkg.dev/project-30b3b95e-ed2b-4573-98a/argus/argus-web:${{ github.sha }} \
            --push \
            .

      - name: Move cache for next run
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache || true

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials "argus-gke" \
            --zone "us-central1-a" --project "project-30b3b95e-ed2b-4573-98a"

      - name: Deploy updated web image
        run: |
          IMAGE="us-central1-docker.pkg.dev/project-30b3b95e-ed2b-4573-98a/argus/argus-web:${{ github.sha }}"
          DEPLOYMENT="argus-web"
          CONTAINER="argus-web"
          kubectl -n argus set image "deployment/${DEPLOYMENT}" "${CONTAINER}=${IMAGE}"
          kubectl -n argus rollout status "deployment/${DEPLOYMENT}" --timeout=180s

      - name: Verify web deployment health
        run: |
          kubectl -n argus wait --for=condition=available deployment/argus-web --timeout=120s
          echo "Web deployment is healthy"