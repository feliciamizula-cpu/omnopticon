You are agent-1 working on a task.

Your state file: /home/derekdperez/omnopticon/tools/.agent-state-agent-1.json
Current task ID: 052
Task description: Enhancement #18: Program/scope export and import - GET /programs/{programId}/export returns JSON with program, scopes, exclusions, rules, rate limit policies. POST /programs/import reconstructs full config. Enables backup/restore, sharing scope templates between programmes, migration between environments, and a scope library. File: src/Services/Argus.ProgramScopeService/

CRITICAL INSTRUCTIONS:
1. FIRST: cat /home/derekdperez/omnopticon/tools/.agent-state-agent-1.json to read your current state and saved context.
2. Treat the state file as the source of truth for progress.
3. Keep the state file current with:
   cd /home/derekdperez/omnopticon/tools && ./agent-coord.sh checkpoint -a agent-1 -t 052 -s working -c "<brief progress summary>"
4. If you pause or get blocked, leave status=working and record the blocker in context or error.
5. Verify: cd /home/derekdperez/omnopticon && dotnet build src/Argus.AppHost/Argus.AppHost.csproj --configuration Release --verbosity quiet
6. On success, commit: git -C /home/derekdperez/omnopticon add -A && git -C /home/derekdperez/omnopticon commit -m "agent-1: Enhancement #18: Program/scope export and import - GET /programs/{programId}/export returns JSON with program, scopes, exclusions, rules, rate limit policies. POST /programs/import reconstructs full config. Enables backup/restore, sharing scope templates between programmes, migration between environments, and a scope library. File: src/Services/Argus.ProgramScopeService/" && git -C /home/derekdperez/omnopticon push origin main
7. Mark done: cd /home/derekdperez/omnopticon/tools && ./agent-coord.sh done -t 052 -a agent-1

Continue from the saved state instead of re-discovering work that has already been done.
