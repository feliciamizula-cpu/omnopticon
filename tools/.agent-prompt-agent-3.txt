You are agent-3 resuming a task after a stop or crash.

Your state file: /home/derekdperez/omnopticon/tools/.agent-state-agent-3.json
Recovered from: agent-3
Current task ID: 056
Task description: Enhancement #22: Asset staleness scoring - Add StalenessScore (int 0-100) to AssetDto computed from (now - LastSeenAt) relative to expected scan frequency for that asset type. UI visual indicator (colour band or age column). Add minStalenessScore/maxStalenessScore to AssetQuery. ScanSchedulerService uses StalenessScore as primary scheduling signal. Files: src/Services/Argus.AssetService/, src/BuildingBlocks/Argus.Contracts/

CRITICAL INSTRUCTIONS:
1. FIRST: cat /home/derekdperez/omnopticon/tools/.agent-state-agent-3.json to read your current state and saved context.
2. Continue from the latest checkpoint instead of restarting the task.
3. Update your state frequently with:
   cd /home/derekdperez/omnopticon/tools && ./agent-coord.sh checkpoint -a agent-3 -t 056 -s working -c "<brief progress summary>"
4. If you need to pause, leave status=working and include your next step in context.
5. Verify: cd /home/derekdperez/omnopticon && dotnet build src/Argus.AppHost/Argus.AppHost.csproj --configuration Release --verbosity quiet
6. On success, commit: git -C /home/derekdperez/omnopticon add -A && git -C /home/derekdperez/omnopticon commit -m "agent-3: Enhancement #22: Asset staleness scoring - Add StalenessScore (int 0-100) to AssetDto computed from (now - LastSeenAt) relative to expected scan frequency for that asset type. UI visual indicator (colour band or age column). Add minStalenessScore/maxStalenessScore to AssetQuery. ScanSchedulerService uses StalenessScore as primary scheduling signal. Files: src/Services/Argus.AssetService/, src/BuildingBlocks/Argus.Contracts/" && git -C /home/derekdperez/omnopticon push origin main
7. Mark done: cd /home/derekdperez/omnopticon/tools && ./agent-coord.sh done -t 056 -a agent-3

When you are finished, clear your state back to idle.
