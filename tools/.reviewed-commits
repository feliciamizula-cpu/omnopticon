b596ff5 Agent framework fixes + ProxyRegistryService
9af0dfe m
0a3e4c4 agent-4: Enhancement #12: Artifact storage - IArtifactStore with LocalFileArtifactStore and S3ArtifactStore
63744d4 agent-3: DevOps: Deploy code review report as web app - Serve tools/reviews/code-review-report.html via a lightweight HTTP server (Python http.server or add Nginx to compose.yaml). Make it accessible on port 8081. Ensure the report auto-updates when generate-review-report.sh runs.
7dd892e agent-5: Enhancement #16: Asset graph traversal API - Add GET /assets/{assetId}/subgraph?maxDepth=3&assetTypes=Subdomain,Url to AssetService. PostgreSQL recursive CTE via Dapper: WITH RECURSIVE reachable AS (SELECT to_asset_id FROM asset_relationships WHERE from_asset_id = rootId UNION ALL SELECT r.to_asset_id FROM asset_relationships r JOIN reachable ON r.from_asset_id = reachable.to_asset_id) SELECT * FROM assets WHERE asset_id IN (SELECT to_asset_id FROM reachable). MaxDepth and assetTypes params. File: src/Services/Argus.AssetService/
c87d548 agent coordination: add task 057 for devops to deploy review web app, start review server on :8081
11f1721 agent-5: Enhancement #13: FindingCandidate promotion workflow - When worker produces FindingCandidate (AssetType with interesting score 70), AssetService emits FindingCandidateCreated event. Build ValidationWorker or manual review: promote to Finding asset type with severity/CVSS fields, or dismiss with reason and Archived status. Add Finding to AssetType enum. Files: src/Services/Argus.AssetService/, src/BuildingBlocks/Argus.Contracts/, src/Workers/Argus.Workers.Validation/
2a6f6ed agent-4: Enhancement #15: Scheduled/recurring scan plans - Add CronExpression field to ScanPlanDto. Create RecurrencePolicy entity. Build ScanSchedulerBackgroundService checking for due recurrences every 60s, auto-creates scan plans from template. Track LastRunAt and NextRunAt on scan plan. Use Cronos NuGet. File: src/Services/Argus.ScanOrchestratorService/
f9ca365 agent-5: Enhancement #13: FindingCandidate promotion workflow - When worker produces FindingCandidate (AssetType with interesting score 70), AssetService emits FindingCandidateCreated event. Build ValidationWorker or manual review: promote to Finding asset type with severity/CVSS fields, or dismiss with reason and Archived status. Add Finding to AssetType enum. Files: src/Services/Argus.AssetService/, src/BuildingBlocks/Argus.Contracts/, src/Workers/Argus.Workers.Validation/
5d5ef85 m
2bd3c5d agent coordination: update task board with all 23 enhancements, add review report generator, menu control center updates
f97621f agent-4: This script that shows menu options has malformed text (bad encoding?) that appears in the menu options
8fff539 agent-5: Enhancement #12: Worker graceful draining on SIGTERM - In ArgusWorkerBackgroundService, override StopAsync to honour configurable drain timeout. Read ARGUS_WORKER_DRAIN_TIMEOUT env var (default 60s). On StopAsync: signal no new tasks, wait for in-flight tasks up to drain timeout. Support checkpoint writing before yielding to cancellation for resume support. Update deploy/compose.yaml workers with stop_grace_period: 90s. File: src/BuildingBlocks/Argus.BuildingBlocks.Workers/ArgusWorkerBackgroundService.cs
bea22eb agent-5: Enhancement #12: Worker graceful draining on SIGTERM - In ArgusWorkerBackgroundService, override StopAsync to honour configurable drain timeout. Read ARGUS_WORKER_DRAIN_TIMEOUT env var (default 60s). On StopAsync: signal no new tasks, wait for in-flight tasks up to drain timeout. Support checkpoint writing before yielding to cancellation for resume support. Update deploy/compose.yaml workers with stop_grace_period: 90s. File: src/BuildingBlocks/Argus.BuildingBlocks.Workers/ArgusWorkerBackgroundService.cs
baa2faa agent-4: DevOps: fix crashed workers
45d106d agent-4: Enhancement #11: Custom operational metrics - shared ArgusMetrics class with counters/histograms tagged by program_id, worker_type, asset_type
